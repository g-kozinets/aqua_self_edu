package framework.pageElements;

import org.openqa.selenium.By;

public class Banner extends BaseElement {

    public Banner(By locator, String elementName) {
        super(locator, elementName);
    }
}
